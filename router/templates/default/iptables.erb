# Generated by iptables-save v1.4.7 on Wed Oct 23 12:40:31 2013
*nat
:PREROUTING ACCEPT [3:295]
:POSTROUTING ACCEPT [0:0]
:OUTPUT ACCEPT [0:0]
-A POSTROUTING -o eth0 -j MASQUERADE
COMMIT
# Completed on Wed Oct 23 12:40:31 2013
# Generated by iptables-save v1.4.7 on Wed Oct 23 12:40:31 2013
*filter
:INPUT DROP [0:0]
:FORWARD DROP [0:0]
:OUTPUT ACCEPT [0:0]
-A INPUT -i lo -j ACCEPT
-A INPUT -i eth0 -m state --state RELATED,ESTABLISHED -j ACCEPT
<% node[:iptables_allowed_subnets].each do |subnet| %>
-A INPUT -i eth0 -s <%= subnet %> -p icmp -j ACCEPT
<% end %>
<% node[:iptables_allowed_subnets].each do |subnet| %>
<% node[:iptables_open_ports][:tcp].each do |port| %>
-A INPUT -i eth0 -s <%= subnet %> -m state --state NEW -m tcp -p tcp --dport <%= port %> -j ACCEPT
<% end %>
<% node[:iptables_open_ports][:udp].each do |port| %>
-A INPUT -i eth0 -s <%= subnet %> -m state --state NEW -m udp -p udp --dport <%= port %> -j ACCEPT
<% end %>
<% end %>
-A INPUT -i eth0 -j REJECT --reject-with icmp-host-prohibited
-A INPUT -i eth1 -j ACCEPT
-A FORWARD -i eth0 -o eth1 -m state --state RELATED,ESTABLISHED -j ACCEPT
<% node[:iptables_allowed_subnets].each do |subnet| %>
-A FORWARD -i eth0 -o eth1 -s <%= subnet %> -p icmp -j ACCEPT
<% end %>
<% node[:iptables_allowed_subnets].each do |subnet| %>
<% node[:iptables_open_ports][:tcp].each do |port| %>
-A FORWARD -i eth0 -o eth1 -s <%= subnet %> -m state --state NEW -m tcp -p tcp --dport <%= port %> -j ACCEPT
<% end %>
<% node[:iptables_open_ports][:udp].each do |port| %>
-A FORWARD -i eth0 -o eth1 -s <%= subnet %> -m state --state NEW -m udp -p udp --dport <%= port %> -j ACCEPT
<% end %>
<% end %>
-A FORWARD -i eth0 -j REJECT --reject-with icmp-host-prohibited
-A FORWARD -i eth1 -o eth0 -j ACCEPT
COMMIT
